<?php

namespace App\Observers;

use App\Models\Order;

class OrderObserver
{
    /**
     * Handle the Order "created" event.
     *
     * @param  \App\Models\Order  $order
     * @return void
     */
    public function created(Order $order)
    {
        // Broadcasting disabled - using polling instead
        // Uncomment below if you want to enable WebSocket broadcasting
        /*
        if (in_array($order->status, ['failed', 'pending']) && $order->is_deleted == 0) {
            \Log::info('OrderObserver: Broadcasting new order', ['order_id' => $order->id, 'status' => $order->status]);
            event(new \App\Events\NewOrderCreated($order));
            \Log::info('OrderObserver: Event fired', ['order_id' => $order->id]);
        }
        */
    }

    /**
     * Handle the Order "updated" event.
     *
     * @param  \App\Models\Order  $order
     * @return void
     */
    public function updated(Order $order)
    {
        //
    }

    /**
     * Handle the Order "deleted" event.
     *
     * @param  \App\Models\Order  $order
     * @return void
     */
    public function deleted(Order $order)
    {
        //
    }

    /**
     * Handle the Order "restored" event.
     *
     * @param  \App\Models\Order  $order
     * @return void
     */
    public function restored(Order $order)
    {
        //
    }

    /**
     * Handle the Order "force deleted" event.
     *
     * @param  \App\Models\Order  $order
     * @return void
     */
    public function forceDeleted(Order $order)
    {
        //
    }
}
